# NJT developer portal credentials (optional — the API does not enforce them;
# leave blank and the client will use empty credentials, which the API accepts).
NJTRANSIT_USERNAME=
NJTRANSIT_PASSWORD=
NJTRANSIT_BASE_URL=https://raildata.njtransit.com/api/TrainData
PORT=8080
